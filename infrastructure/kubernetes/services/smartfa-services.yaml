apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: smartfa
  labels:
    app: smartfa-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: smartfa-frontend

---
apiVersion: v1
kind: Service
metadata:
  name: multimodal-workbench-service
  namespace: smartfa
  labels:
    app: smartfa-multimodal-workbench
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: smartfa-multimodal-workbench

---
apiVersion: v1
kind: Service
metadata:
  name: intelligent-hub-service
  namespace: smartfa
  labels:
    app: smartfa-intelligent-hub
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: smartfa-intelligent-hub

---
apiVersion: v1
kind: Service
metadata:
  name: multi-agent-cluster-service
  namespace: smartfa
  labels:
    app: smartfa-multi-agent-cluster
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: smartfa-multi-agent-cluster

---
apiVersion: v1
kind: Service
metadata:
  name: image-analysis-service
  namespace: smartfa
  labels:
    app: smartfa-image-analysis
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: smartfa-image-analysis

---
apiVersion: v1
kind: Service
metadata:
  name: document-analysis-service
  namespace: smartfa
  labels:
    app: smartfa-document-analysis
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: smartfa-document-analysis

---
apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: smartfa
  labels:
    app: smartfa-llm-service
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: smartfa-llm-service